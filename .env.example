LLM_PROVIDER=anthropic        # anthropic | openai | none (solo plan B por reglas)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DB_PATH=data/hospital.db
ACCESS_PATH=data/access.json   # usuarios y permisos por rol (se crea solo)
AUDIT_PATH=data/audit.jsonl    # bitácora de auditoría de consultas e informes (se crea sola)
